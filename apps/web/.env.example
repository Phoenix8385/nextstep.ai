# Backend API base URL (public: baked into the browser bundle).
NEXT_PUBLIC_API_URL=http://localhost:8000

# NextAuth: generate with `openssl rand -base64 32`. Never reuse across environments.
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=change-me-to-a-long-random-string
